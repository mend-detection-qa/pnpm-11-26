# Auto-install peers so the react peer dep on @probe/shared
# is fully materialized in the lockfile.
auto-install-peers=true

# Trust public scripts — exercises the pnpm 11.26 trust-flag
# behavior for packages with lifecycle scripts (esbuild).
trust-public-scripts=true

# Strict isolation (default pnpm behavior, made explicit).
node-linker=isolated
